{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Program",
			"preLaunchTask": "Build",
			"request": "launch",
			"type": "node",
			"program": "${workspaceFolder}/bin/Mc2it.Theme.js",
			"args": ["${input:command}"],
			"env": {"NODE_ENV": "development"}
		}
	],
	"inputs": [
    {
			"description": "The command to invoke.",
      "id": "command",
      "type": "promptString"
    }
  ]
}
