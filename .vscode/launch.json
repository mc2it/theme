{
	"version": "0.2.0",
	"configurations": [
		{
			"name": "Program",
			"preLaunchTask": "Debug",
			"request": "launch",
			"type": "node",
			"program": "${workspaceFolder}/bin/Mc2it.Theme.js",
			"args": ["${input:command}"]
		}
	],
	"inputs": [
    {
			"description": "The command to invoke.",
      "id": "command",
      "type": "promptString"
    }
  ]
}
