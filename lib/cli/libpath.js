import {Command} from "commander";

/**
 * Command printing the path to the library assets.
 * @type {Command}
 */
const command = new Command("libpath");
//command.addOption();

export default command;
